%tr{class: "#{ host.monitored == 1 ? '' : 'danger' }"}
  %td
    = partial(:dots, locals:{object: host})
    = host.name
  %td
    = host.host_name
  %td
    = host.port_number
  %td
    = icon_for_protocol(host.protocol)
    = host.protocol
  %td.text-right
    = host.response_time
